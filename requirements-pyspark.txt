# PySpark + Koalas (pyspark.pandas)
# Note: Koalas is now bundled inside PySpark >= 3.2 as pyspark.pandas
# PySpark 3.x requires Java 11+; PySpark 4.x requires Java 17.
# Pin to 3.x so the VM's Java 11 install works without upgrading Java.
pyspark>=3.4.0,<4.0.0
py4j>=0.10.9.7

# Storage
gcsfs>=2023.9.0
google-cloud-storage>=2.10.0

# Includes all base requirements
-r requirements.txt
